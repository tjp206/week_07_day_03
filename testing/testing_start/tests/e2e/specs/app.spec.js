describe('App', () => {
  beforeEach(() => {
  })

  it('Should have populated select', () => {
  })

  it('should show selected country on select change', () => {
      })

  xit('should add country to favourties on button click', () => {
  })
})
